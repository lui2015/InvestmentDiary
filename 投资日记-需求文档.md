# 投资日记 · 产品需求文档（PRD）

> 版本：v1.0
> 日期：2026-08-17
> 状态：初稿 / 待评审

---

## 1. 项目概述

### 1.1 背景
用户在日常进行股票、基金、期货等多品类投资时，缺乏一个统一、轻量、可随时记录与复盘的工具。现有市面产品要么偏重行情分析、要么操作繁琐，难以沉淀"我到底做了哪些操作、赚亏多少、当时为什么这么想"这类第一人称数据。

"投资日记"定位为**个人投资行为记录与复盘工具**，核心价值是把分散在各券商 App 里的买卖动作、盈亏结果，以及用户当时的决策逻辑，集中记录下来，并通过止盈止损提醒和投资复盘功能，帮助用户形成闭环的投资习惯。

### 1.2 目标
- 用一套账号体系，安全记录用户在**股票 / 基金 / 期货**三个品类的投资行为。
- 自动统计单笔、单标的、单账户及总体盈亏收益。
- 提供可配置的止盈止损提醒，降低情绪化持仓风险。
- 结构化的复盘模板，帮助用户定期回顾决策质量。
- 一套代码同时完美适配桌面与移动端，随时随手记录。

### 1.3 非目标（本期不做）
- 不接入券商自动同步交易流水（本期手动录入，后续可扩展）。
- 不提供实时行情、交易下单、投顾建议等资质敏感功能。
- 不做社区/分享/社交功能。

---

## 2. 用户角色

| 角色 | 说明 | 核心诉求 |
|------|------|----------|
| 普通用户 | 注册登录后独立管理自己的投资日记 | 记录、统计、提醒、复盘 |
| （预留）管理员 | 系统运维、内容维护 | 用户数据合规管理、公告 |

> 本期仅实现"普通用户"单角色；数据严格按账号隔离，A 用户不可见 B 用户数据。

---

## 3. 功能需求

### 3.1 账号体系

#### FR-1 注册
- 支持用户名 + 密码注册（用户名唯一，3–20 位；密码强度校验，至少 8 位且含字母+数字）。
- 注册成功后自动登录。
- 密码以 bcrypt/scrypt 等单向哈希存储，**明文密码不上库**。

#### FR-2 登录 / 登出
- 用户名 + 密码登录，错误次数限制（连续 5 次失败锁定 15 分钟）防暴力破解。
- 登录态使用 HttpOnly + Secure + SameSite 的会话 Cookie（或 JWT），有效期可配置。
- 支持"记住我"（延长会话时长）。
- 提供登出，清除会话。

#### FR-3 账号安全
- 密码修改（需校验原密码）。
- 会话管理：多端登录可配置为"互踢"或"共存"。
- 关键操作（修改密码）需登录态校验。

#### FR-4 密码找回（可选，建议二期）
- 预留邮箱/手机号绑定字段，本期可仅做占位。

---

### 3.2 投资行为记录

#### FR-5 标的与账户管理
- 用户可维护自己的**交易账户**（如"华泰证券""支付宝基金""期货公司 A"），用于归集交易。
- 用户可录入**标的**，字段随品类差异化：

| 品类 | 关键字段 |
|------|----------|
| 股票 | 代码、名称、市场（A股/港股/美股）、所属账户 |
| 基金 | 代码、名称、类型（股票型/债券型/混合型/指数型）、所属账户 |
| 期货 | 合约代码、名称、交易所、多空方向、杠杆、所属账户 |

#### FR-6 交易记录录入
- 每笔记录为一次**开仓 / 加仓 / 减仓 / 平仓 / 分红 / 手续费**动作。
- 通用字段：日期时间、品类、标的、账户、方向（买/卖）、数量、价格、金额、手续费、备注。
- 期货专属：开/平、多/空、保证金、杠杆倍数。
- 支持**草稿暂存**与**历史编辑 / 删除**（删除需二次确认）。
- 录入数据合法性校验（数量/价格为正、卖出不超持仓等）。

#### FR-7 持仓计算
- 系统根据同一标的的买卖流水，计算：
  - 当前持仓数量、持仓成本（移动加权平均）、持仓市值（需用户输入最新市价或手动更新）。
  - 浮动盈亏 = 市值 − 持仓成本。
- 期货按方向计算浮动盈亏（多：价差×手数×合约乘数；空：反向）。

---

### 3.3 盈亏收益统计

#### FR-8 多维度统计
- **单笔盈亏**：已实现盈亏（平仓时结算）。
- **单标的**：累计盈亏、收益率。
- **单账户 / 全账户**：总资产、总投入、总盈亏、收益率。
- **时间维度**：按日/月/年汇总盈亏曲线。

#### FR-9 数据可视化
- 总览仪表盘：总资产卡片、累计收益率、近期盈亏走势折线图。
- 品类分布饼图（股票/基金/期货占比）。
- 各标的盈亏排行（盈利榜 / 亏损榜）。

#### FR-10 导出
- 支持将交易记录、统计结果导出为 CSV / Excel，便于备份与报税。

---

### 3.4 止盈止损提醒

#### FR-11 提醒规则配置
- 用户对每个持仓标的可设置：
  - **止损线**：当浮动亏损达到设定比例（如 −10%）或跌破设定价格时提醒。
  - **止盈线**：当浮动盈利达到设定比例（如 +20%）或涨破设定价格时提醒。
- 规则关联"当前市价"计算触发条件（市价由用户手动维护或后续接入行情）。

#### FR-12 提醒触达
- 站内提醒（登录后在"提醒中心"红点展示，含标的信息、触发类型、建议动作）。
- 支持推送（二期：邮件 / 浏览器通知 / 微信）。
- 提醒可标记"已处理 / 已忽略"，避免重复打扰。

#### FR-13 提醒策略
- 触发后冷却期内不重复提醒（如同一规则 24 小时内仅提醒一次）。

---

### 3.5 投资复盘

#### FR-14 复盘模板
- 提供结构化复盘模板，引导用户回答：
  - 本期操作回顾（做了什么买卖）。
  - 决策逻辑（为什么买/卖，依据是什么）。
  - 结果评估（符合预期吗，偏差原因）。
  - 情绪与纪律（是否受情绪影响、是否遵守计划）。
  - 经验沉淀（下次改进点）。
- 模板可自定义字段（二期）。

#### FR-15 复盘管理
- 复盘按周期组织：周复盘 / 月复盘 / 自定义时间段。
- 可关联该时间段内的交易记录，自动带入操作清单。
- 支持富文本/标签，支持历史复盘检索与对比。

#### FR-16 复盘统计
- 统计"计划执行率""盈利交易占比"等纪律指标，帮助识别自身投资行为模式。

---

### 3.6 通知与系统

#### FR-17 站内消息
- 提醒中心统一承载止盈止损提醒、系统公告。

#### FR-18 数据备份
- 用户可一键导出全部数据（JSON），支持后续导入恢复（二期）。

---

### 3.7 个性化主题（页面换肤）

#### FR-19 主题切换
- 用户可在"我的 / 外观"中切换页面主题，系统内置多套风格，初期至少包含：
  - **默认（浅色 / 经典）**：干净明亮的常规风格。
  - **暗色（Dark）**：低亮度护眼风格，适合夜间使用。
  - **赛博朋克（Cyberpunk）**：霓虹高对比、青紫/品红配色、发光边框与扫描线质感的科技风。
  - **漫画风格（Comic）**：粗描边、网点底纹、活泼配色、气泡化卡片的卡通手绘风。
- 切换即时生效，全站统一换肤（含仪表盘、表单、图表、弹窗等所有组件）。
- 主题仅改变视觉表现（色彩 / 字体 / 装饰），不改变任何功能与布局结构。

#### FR-20 主题持久化与跟随系统
- 用户所选主题持久化保存（存入账号偏好，登录后自动应用；未登录可用本地存储临时记忆）。
- 提供"跟随系统"选项：根据操作系统的浅色 / 深色模式自动切换。
- 切换时尊重 `prefers-reduced-motion` 等无障碍设置，避免过动效引发不适。

#### FR-21 主题扩展
- 主题以"设计令牌（Design Token）/ CSS 变量"方案实现，新增主题只需补充一套变量配置，无需改动业务组件，便于后续持续扩展更多风格（如国风、像素风等）。

---

## 4. 非功能需求

### 4.1 响应式与多端适配（核心诉求）
- 采用**移动优先（Mobile-First）响应式设计**，断点建议：
  - 手机 `< 768px`：单列流式布局，底部 Tab 导航，大点击区域。
  - 平板 `768–1024px`：自适应网格。
  - 桌面 `≥ 1024px`：侧边栏 + 多列仪表盘。
- 同一套前端代码自适应，禁止为两端维护两套页面。
- 触控友好：按钮 ≥ 44×44px，表单输入适配移动键盘类型（数字键盘等）。
- 在主流浏览器（Chrome / Safari / Edge）与 iOS / Android 真机验证。

### 4.2 性能
- 首屏加载 < 2s（4G 网络），列表分页 / 虚拟滚动处理大量交易记录。
- 统计聚合接口响应 < 500ms。

### 4.3 安全
- 登录态 HttpOnly + Secure Cookie 或短期 JWT。
- 全站 HTTPS，敏感接口加 CSRF 防护。
- 用户输入做转义防 XSS；SQL 使用参数化查询防注入。
- 会话与权限校验：所有数据接口必须校验当前用户归属。
- 密码单向哈希；密钥仅存环境变量。

### 4.4 可用性 / 可维护性
- 清晰的错误处理与空状态引导（首次使用引导录入第一笔交易）。
- 数据本地校验 + 友好错误提示。
- 日志与监控（登录异常、接口错误）。

### 4.5 合规
- 明确告知用户本工具为**个人记录辅助**，不构成投资建议。
- 用户数据归属用户，提供导出与删除（账号注销）能力。

---

## 5. 数据模型（概要）

> 以下为逻辑实体，实现可使用关系型数据库（如 SQLite / PostgreSQL）+ ORM。

| 实体 | 关键字段 |
|------|----------|
| `User` | id, username(unique), password_hash, created_at, status |
| `Account` | id, user_id, name, broker, type, created_at |
| `Symbol` | id, user_id, category(stock/fund/future), code, name, market, extra(期货方向/杠杆等) |
| `Trade` | id, user_id, account_id, symbol_id, action(open/add/reduce/close/dividend/fee), side(buy/sell), quantity, price, amount, fee, datetime, note |
| `Position` | 视图/派生：symbol_id, hold_qty, avg_cost, market_price, float_pnl（可由 Trade 实时计算或缓存） |
| `AlertRule` | id, user_id, symbol_id, type(stop_profit/stop_loss), mode(percent/price), threshold, status, last_triggered |
| `AlertLog` | id, rule_id, triggered_at, message, handled |
| `Review` | id, user_id, period_type, start_date, end_date, content(json), created_at |
| `UserPref` | id, user_id, theme(light/dark/cyberpunk/comic/auto), language, created_at, updated_at |

---

## 6. 技术架构建议

### 6.1 推荐方案
- **前端**：React / Vue + TypeScript + Vite；UI 组件库（如 Ant Design / Element Plus，移动端用其移动适配或 Tailwind 自适配）。图表用 ECharts / Recharts。**主题采用 CSS 变量（Design Token）+ 主题类名（如 `theme-cyberpunk`）方案**，全局切换 `<html>` 上的主题属性即可整站换肤。
- **后端**：Node.js（Express / NestJS）或 Python（FastAPI），提供 REST API。
- **存储**：PostgreSQL（或 SQLite 起步）；会话用 JWT / 服务端 session。
- **鉴权**：bcrypt 密码哈希 + 会话中间件。
- **部署**：容器化（Docker），反向代理（Nginx）处理 HTTPS 与静态资源；支持子路径部署（参考现有项目 /fundAnalysis 等做法）。
- **提醒引擎**：定时任务扫描 `AlertRule` × 最新市价，命中则写 `AlertLog` 并推站内。

### 6.2 目录 / 接口约定（草案）
- `POST /api/auth/register|login|logout`
- `GET/PUT /api/user/profile`
- `CRUD /api/accounts`, `/api/symbols`, `/api/trades`
- `GET /api/stats/overview|by-symbol|by-period`
- `CRUD /api/alerts`，`GET /api/alerts/logs`
- `CRUD /api/reviews`，`GET /api/reviews/template`

> 接口统一返回 `{ code, message, data }`，需登录接口返回 401 引导登录。

---

## 7. 页面与交互（信息架构）

1. **登录 / 注册页**（移动端卡片式，桌面端居中卡片）。
2. **总览仪表盘**：资产卡片 + 盈亏曲线 + 品类分布 + 待处理提醒。
3. **交易记录**：列表（可按品类/账户/时间筛选）+ 新增/编辑表单（品类差异化字段）。
4. **持仓**：当前持仓表 + 浮动盈亏，可设置止盈止损。
5. **止盈止损提醒**：规则管理 + 提醒中心。
6. **复盘**：复盘列表 + 新建复盘（模板引导）+ 历史对比。
7. **我的**：账号信息、修改密码、数据导出、关于/免责声明。
8. **外观 / 主题**：主题切换入口（默认/暗色/赛博朋克/漫画风格/跟随系统），实时预览。

---

## 8. 里程碑（建议）

| 阶段 | 内容 | 交付 |
|------|------|------|
| M1 | 账号体系 + 基础框架 + 响应式骨架 + 主题框架（CSS变量/令牌） | 可登录、可录入交易、可切换主题 |
| M1.5 | 内置主题打磨（默认/暗色/赛博朋克/漫画风格） | 多风格可用、登录后持久化 |
| M2 | 交易记录 + 持仓 + 盈亏统计 + 可视化 | 完整记录与统计闭环 |
| M3 | 止盈止损提醒 | 规则配置 + 站内提醒 |
| M4 | 投资复盘 | 模板 + 复盘管理 + 纪律统计 |
| M5 | 导出/备份、性能与安全加固、多端真机验收 | 可上线版本 |

---

## 9. 验收标准（核心诉求对照）

| 用户诉求 | 验收要点 |
|----------|----------|
| 记录股票/基金/期货并统计盈亏 | 三类标的均可录入，总览与各维度盈亏正确 |
| 止盈止损提醒 | 可配置规则，触发后站内可见且不过度打扰 |
| 投资复盘 | 结构化模板可用，可关联交易、可检索 |
| 账号体系（账号密码登录） | 注册/登录/登出/改密完整，数据隔离 |
| 完美适配电脑&手机 | 同一套代码在桌面与移动端均布局合理、可操作 |
| 可切换页面主题（赛博朋克/漫画风格等） | 内置多套主题可即时切换、登录后持久化、各端显示一致且不影响功能 |

---

*文档后续可补充：交互原型图、API 详细字段、测试用例、UI 设计规范。*
